<?xml version="1.0" encoding="utf-8"?>
<!-- Grid tile of the search screen. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:orientation="vertical">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardView"
        style="@style/Widget.Cine.Card.Poster"
        android:layout_width="match_parent"
        android:layout_height="248dp"
        android:foreground="?attr/selectableItemBackground">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/posterSearch"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/placeholder_poster"
                android:contentDescription="@string/cd_poster"
                android:scaleType="centerCrop"
                tools:src="@drawable/abc" />

            <TextView
                android:id="@+id/txtImdb"
                style="@style/Widget.Cine.RatingChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|start"
                android:layout_margin="8dp"
                tools:text="7.9" />
        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>

</LinearLayout>
